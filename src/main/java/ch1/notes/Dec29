ArrayLists와 Hashmap은 싱글스레드에서 안전하다. 자원의 값이 바뀔수있다.

Map은 키와 값을 쌍으로 묶어 관리하는 구조 // Map<키(String), 값(Object)>

List 컬렉션 = 객체를 일렬로 늘어놓은 구조
객체를 인덱스로 관리하기 때문에 객체를 저장하면 자동으로 인덱스가 부여, 인덱스로 객체를 검색, 삭제 가능
객체 자체를 저장X 객체의 번지를 참조. 동일 객체 중복저장가능 -> 이럴때 동일번지 참조.
Null 역시도 저장가능 -> 이럴때 해당 인덱스는 객체 참조 X
